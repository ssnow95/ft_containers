#include "list.hpp"


int main()
{
	ft::list<int> second ((size_t)4,100);                       // four ints with value 100
  	// ft::list<int> third (second.begin(),second.end());
	// int myints[] = {16,2,77,29};
  	// ft::list<int> b (myints, myints + sizeof(myints) / sizeof(int) );
	// ft::list<int> a = {10, 10};
	// ft::list<int> a ((size_t)7, 10);
// ft::list<int> a;
}